//alert(Array.from(document.body.childNodes).filter())

const text = document.querySelector('.lesson__text');

const textElementContent = text.innerHTML;
text.innerHTML = `
    <h1>What the fuck Jhony?</h1>
    <p class="yellow-text">We should be go to the cinema!</p>
`;



